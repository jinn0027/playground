Bootstrap: localimage
From: ../common/el9.sif

%setup

%files

%post

    dnf install -y \
        emacs

    pushd /opt
        # install python-docx
        /opt/miniconda3/bin/mamba install -y -q python-docx=1.2.0
    popd

%environment
    export LANG="ja_JP.UTF-8"
    export LC_ALL="ja_JP.UTF-8"

%runscript
